<view class="page">
  <view class="card">
    <view class="row">
      <view class="label">头像</view>
      <button class="value avatar-wrap" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
        <image class="avatar" src="{{avatar || defaultAvatar}}" mode="aspectFill" />
        <text class="tip">点击更换</text>
      </button>
    </view>

    <view class="row">
      <view class="label">昵称</view>
      <input class="input" type="nickname" value="{{nickname}}" placeholder="点击获取微信用户名" bindinput="onNicknameInput" maxlength="20" />
    </view>

    <view class="row">
      <view class="label">手机号</view>
      <view class="value phone-wrap">
        <text class="phone">{{phone || '未绑定'}}</text>
        <button class="btn small" open-type="getPhoneNumber" bindgetphonenumber="onGetPhoneNumber">绑定</button>
      </view>
    </view>

    <view class="actions">
      <button class="btn" bindtap="onCancel">取消</button>
      <button class="btn primary" bindtap="onSave">保存</button>
    </view>
  </view>
</view>

